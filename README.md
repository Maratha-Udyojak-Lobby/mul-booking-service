# MUL Booking Service

Appointments and table reservations management.

## Development

```bash
pip install -r requirements.txt
python -m uvicorn app.main:app --reload --port 8003
```

Server runs on http://127.0.0.1:8003
